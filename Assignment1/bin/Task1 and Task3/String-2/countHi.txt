public int countHi(String str) {
  int count=0;
  for(int i=0;i<str.length()-1;i++){
    String hi=String.valueOf(str.charAt(i))+String.valueOf(str.charAt(i+1));
    if(hi.equals("hi"))
      count++;
  }
  return count;
}