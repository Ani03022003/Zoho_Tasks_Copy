//With loop
public boolean prefixAgain(String str, int n) {
  for(int i=n;i<=str.length()-n;i++){
    String s=str.substring(i,n+i);
    if(s.equals(str.substring(0,n))){
      return true;
    }
  }
  return false;
}


//Without loop
  String prefix=str.substring(0,n);
  String suffix=str.substring(n,str.length());
  if(suffix.contains(prefix)){
    return true;
  }
  return false;