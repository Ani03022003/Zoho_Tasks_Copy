public int countClumps(int[] nums) {
  int count=0;
  boolean flag=false;
  for(int i=0;i<nums.length-1;i++){
    if(nums[i]==nums[i+1] && !flag){
      count++;
      flag=true;
    }
    if(nums[i]!=nums[i+1] && flag){
      flag=false;
    }
  }
  return count;
}
