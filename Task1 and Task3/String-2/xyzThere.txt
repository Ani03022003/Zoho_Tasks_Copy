public boolean xyzThere(String str) {
  for(int i=0;i<str.length()-2;i++){
    String xyz=str.substring(i,3+i);
    if(xyz.equals("xyz")){
      if(i==0 || str.charAt(i-1)!='.'){
        return true;
      }
    }
  }
  return false;
}
