public boolean nearHundred(int n) {
  if(n<0)
    return false;
  if(Math.abs(n)>=90 && Math.abs(n)<=110)
    return true;
  if(Math.abs(n)>=190 && Math.abs(n)<=210)
    return true;
  return false;
}